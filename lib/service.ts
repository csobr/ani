import axios from 'axios';

// const fetchFromCMS = async (path) => {
//   const url = `http://localhost:9000/${path}${path.preview ? 'preview' : ''}`;
//   const res = await axios.get(url);
//   return res.data;
// };

// export default fetchFromCMS;
